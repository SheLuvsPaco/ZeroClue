# ZeroChat Environment Configuration
# Copy this file to .env and update with your values

# ====== Database Configuration ======
POSTGRES_USER=zerochat_user
POSTGRES_PASSWORD=zerochat_dev_password_2024
POSTGRES_DB=zerochat

# Database connection URL (used by server)
DATABASE_URL=postgresql://zerochat_user:zerochat_dev_password_2024@localhost:5432/zerochat

# ====== Redis Configuration ======
REDIS_URL=redis://localhost:6379

# ====== Server Configuration ======
# The public URL where your server is accessible
#BASE_PUBLIC_URL=http://localhost:8080
BASE_PUBLIC_URL=https://joya-pentadactyl-lin.ngrok-free.dev

# Server port
PORT=8080

# Rust log level (trace, debug, info, warn, error)
RUST_LOG=info,zerochat=debug

# ====== Security ======
# JWT secret (generate a strong random string for production)
# You can generate one with: openssl rand -base64 32
JWT_SECRET=change_me_in_production_use_openssl_rand

# ====== Production Settings ======
# For production, change these:
# - Use strong passwords (minimum 32 characters)
# - Use HTTPS for BASE_PUBLIC_URL
# - Set RUST_LOG=info (not debug)
# - Generate a new JWT_SECRET
# - Consider using environment-specific configs
